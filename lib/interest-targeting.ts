/**
 * Shared types for Interest Targeting (multi-select with search).
 * Each platform provides its own options list; this file only defines the shape.
 */

export interface InterestOption {
  id: string;
  label: string;
}
